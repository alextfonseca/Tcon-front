import React, {useState, useEffect} from 'react';
import { ScrollView, Text, TouchableOpacity, View } from "react-native";

import axios from 'axios';

import { styles } from "./styles"

export const Ranking = () => {
    return <Text>XAU</Text>
}